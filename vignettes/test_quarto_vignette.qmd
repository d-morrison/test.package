---
title: "test_quarto_vignette"
vignette: >
  %\VignetteIndexEntry{test_quarto_vignette}
  %\VignetteEngine{quarto::html}
  %\VignetteEncoding{UTF-8}
knitr:
  opts_chunk:
    collapse: true
    comment: '#>'
---

```{r}
#| label: setup

```

# get data from a private page

<https://osf.io/deq3w> contains a copy of `iris`:

```{r}
#| eval: false
library(osfr)
library(dplyr)
# 
iris2 <- osf_retrieve_file("deq3w") |> 
  osf_download(path = tempdir(), conflicts = "overwrite") |> 
  pull("local_path") |> 
  read.csv()

iris2
```

get data from Serocalculator:

```{r}

library(dplyr)
temp <- osf_retrieve_file("n6cp3") |> 
  osf_download(path = tempdir(), conflicts = "overwrite") |> 
  pull("local_path") |> 
  readr::read_rds()

temp

```


hi
